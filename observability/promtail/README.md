Promtail collects container logs from Kubernetes nodes and forwards them to Loki.

For local development it is deployed together with the Grafana Loki monolithic deployment.

In production Promtail configuration can be extended with additional scrape jobs and labels.
