apiVersion: v1
kind: Service
metadata:
  name: alert-webhook-receiver
  namespace: monitoring
  labels:
    app: alert-webhook-receiver
spec:
  selector:
    app: alert-webhook-receiver
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
  type: ClusterIP
