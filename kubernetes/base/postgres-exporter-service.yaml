apiVersion: v1
kind: Service
metadata:
  name: postgres-exporter
  namespace: telemetry-system
  labels:
    app: postgres-exporter
spec:
  selector:
    app: postgres-exporter
  ports:
    - name: metrics
      port: 9187
      targetPort: metrics
      protocol: TCP
  type: ClusterIP
