name: Build and Push Docker Images


on:
  push:
    branches:
      - main


permissions:
  contents: read
  packages: write


jobs:
  docker:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses:  actions/checkout@v4

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push ingestion-api
        uses: docker/build-push-action@v6
        with:
          context: ./services/ingestion-api
          push: true
          tags: |
            ghcr.io/av-crypto-security/ingestion-api:v1.1.0
            ghcr.io/av-crypto-security/ingestion-api:latest

      - name: Build and push processing-service
        uses: docker/build-push-action@v6
        with:
          context: ./services/processing-service
          push: true
          tags: |
            ghcr.io/av-crypto-security/processing-service:v1.1.0
            ghcr.io/av-crypto-security/processing-service:latest

      - name: Build and push simulator
        uses: docker/build-push-action@v6
        with:
          context: ./services/simulator
          push: true
          tags: |
            ghcr.io/av-crypto-security/simulator:v1.0.0
            ghcr.io/av-crypto-security/simulator:latest
